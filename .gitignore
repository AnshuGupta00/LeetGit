# 1. Add this to .gitignore
.env
.env.*
!.env.example

# 2. Remove .env from Git tracking (NOT from your computer)
git rm --cached .env

# 3. Commit
git add .gitignore
git commit -m "Remove env file from repository"

# 4. Push
git push